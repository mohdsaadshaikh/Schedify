export const DEFAULT_REDIRECT = "/profile/edit";

export const publicRoutes = ["/verification"];

export const authRoutes = [
  "/login",
  "/register",
  "/error",
  "/forgot-password",
  "/reset-password",
];

export const authApiPrefix = "/api/auth";
